# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/groundwater_mapper?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Python Microservice URL
# For local development: http://localhost:5000
# For production (Render): https://groundwater-mapper.onrender.com
# IMPORTANT: This is required for handling files larger than 4.5MB (Vercel's limit)
NEXT_PUBLIC_PYTHON_SERVICE_URL="http://localhost:5000"

# LlamaParse API (for Excel processing)
LLAMA_CLOUD_API_KEY="your-llama-cloud-api-key"

# Google Earth Engine (optional)
EARTHENGINE_TOKEN=""

# ===========================================
# Firebase Configuration (Client-side)
# These are public and safe to expose in the browser
# Get these from Firebase Console > Project Settings > Your Apps
# ===========================================
NEXT_PUBLIC_FIREBASE_API_KEY="your-firebase-api-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="your-measurement-id"

# ===========================================
# Firebase Admin SDK (Server-side)
# This is SECRET - never expose in client-side code
# Get this from Firebase Console > Project Settings > Service Accounts
# Copy the entire JSON content and paste it as a single line string
# ===========================================
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'
